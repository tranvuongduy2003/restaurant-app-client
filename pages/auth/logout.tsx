import * as React from 'react';

interface ILogoutPageProps {}

const LogoutPage: React.FunctionComponent<ILogoutPageProps> = (props) => {
  return <div>Logout Page</div>;
};

export default LogoutPage;
